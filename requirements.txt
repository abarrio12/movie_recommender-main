# Needed by the app (app.py)
Flask==3.1.0
requests==2.32.3
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1

# Production server used by the Dockerfile (instead of Flask's development server)
gunicorn==23.0.0

# Needed by the notebook (PorterStemmer). The app doesn't use it, but this way
# a single `pip install -r requirements.txt` covers the whole project.
nltk==3.9.1

# Uses the operating system's certificate store for HTTPS (fixes SSL errors behind antivirus/proxies)
truststore>=0.10