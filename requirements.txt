# Necesarios para la app (app.py)
Flask==3.1.0
requests==2.32.3
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1

# Servidor de producción que usa el Dockerfile (en lugar del servidor de desarrollo de Flask)
gunicorn==23.0.0

# Necesario para el notebook (PorterStemmer). La app no lo usa, pero así
# un solo `pip install -r requirements.txt` sirve para todo el proyecto.
nltk==3.9.1
